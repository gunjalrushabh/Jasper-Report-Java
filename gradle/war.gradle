apply plugin: "war"

bootWar {
    mainClass = "com.alphaware.CustomerDiginextApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
